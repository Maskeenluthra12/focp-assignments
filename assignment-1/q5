#include <stdio.h>
int main() 
{
    //ques 5
    int num,sum;
    printf("Enter a number:\n");
    scanf("%d",&num);
    for(int i=1; i<num;i++)
    {if(num%i==0)
        sum+=i;}
    if(sum==num)
    printf("Perfect Number");
    else
    printf("Not Perfect No.");
    return 0;
}
